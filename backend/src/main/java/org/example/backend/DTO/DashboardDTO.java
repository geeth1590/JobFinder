package org.example.backend.DTO;

public class DashboardDTO {

//    dto.companies = companyRepo.findAll(); // Only companies
//    dto.users = userRepo.findAll().stream()
//    .filter(user -> !(user instanceof Company))
//            .toList(); // Exclude companies from general users

}
